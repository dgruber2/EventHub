{
  "name": "localeventshub",
  "version": "1.0.0",
  "main": "Developement_LocalEventsHub.js",
  "scripts": {
    "test": "jest", // Update the test script to use Jest
    "start": "node Developement_LocalEventsHub.js" // Optional: Add a start script if not already present
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "express": "^4.21.1",
    "helmet": "^8.0.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.8.1"
  },
  "devDependencies": {
    "chai": "^5.1.2",
    "mocha": "^10.8.2",
    "supertest": "^7.0.0",
    "jest": "^29.0.0" // Add Jest as a dev dependency
  },
  "jest": { // Jest configuration
    "testEnvironment": "node", // Set the test environment to Node.js
    "verbose": true, // Optional: Enable verbose test output
    "collectCoverage": true, // Optional: Enable code coverage collection
    "coverageDirectory": "./coverage", // Optional: Specify the coverage report directory
    "testMatch": [ // Optional: Define patterns for test file matching
      "**/__tests__/**/*.js",
      "**/?(*.)+(spec|test).js"
    ]
  }
}
